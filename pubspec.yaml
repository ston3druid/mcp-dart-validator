name: dart_validation_mcp
description: Simplified Dart validation tools using dart analyze
version: 1.0.0
homepage: https://github.com/example/dart_validation_mcp

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dart_mcp_tools: ^0.1.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

executables:
  dart_mcp_tools: dart_mcp_tools

# Remove all the old complex validation files - we now use dart analyze
# This reduces the codebase from ~4000 lines to ~200 lines
