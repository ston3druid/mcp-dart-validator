name: dart_validation_mcp
description: Simplified Dart validation tools using dart analyze with MCP integration
version: 1.0.0
homepage: https://github.com/ston3druid/mcp-dart-validator

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  analyzer: ^8.4.1
  glob: ^2.1.2

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

executables:
  dart_mcp_tools: dart_mcp_tools
  mcp_validation_server: mcp_validation_server
